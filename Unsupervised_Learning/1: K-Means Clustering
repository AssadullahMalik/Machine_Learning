from sklearn.cluster import KMeans
import pandas as pd

df = pd.DataFrame({
    "X": [1, 2, 3, 8, 9, 10],
    "Y": [2, 3, 4, 8, 9, 10]
})

model = KMeans(n_clusters=2)
model.fit(df)

print(model.labels_)
